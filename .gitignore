# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# Environnements virtuels
venv/
.venv/
env/
ENV/

# Jupyter
# On VERSIONNE les notebooks (rapport = livrable imposé) : on n'ignore QUE les checkpoints.
.ipynb_checkpoints/

# IDE / éditeurs
.idea/
.vscode/

# OS / WSL / Windows
.DS_Store
Thumbs.db
*:Zone.Identifier

# Expérimentation
# Les modèles entraînés (saved_models/) sont VERSIONNÉS : livrable imposé, à rendre
# prêts à réexécuter. On n'ignore que les logs. À ajuster si des artefacts lourds
# (gros CSV, images) doivent être exclus par la suite.
experiments/*.log
documents/
.claude/